package dsf19Exam2Version2;

public class cscsc {

	/*
	 * Consider the following code fragment:
	 * 
	 * public void printQueue(DoublyLinkedQueue<Integer> Q){
	 * 
	 * int len = Q.size();
	 * 
	 * int k = 0;
	 * 
	 * for (int i=0; i < len; ++i){
	 * 
	 * k = Q.dequeue();
	 * 
	 * Q.enqueue(k);
	 * 
	 * System.out.println(Q.dequeue());
	 * 
	 * }
	 * 
	 * }
	 * 
	 * What is the complexity of this code?
	 * 
	 * 
	 * 
	 * Select one: a. None of the alternatives is correct. b. Q(1) c. O(N), N = k d.
	 * Q(N2), N = Q.size() e. O(M), M = Q.size() Feedback Your answer is correct.
	 * The correct answer is: O(M), M = Q.size()
	 */

}
