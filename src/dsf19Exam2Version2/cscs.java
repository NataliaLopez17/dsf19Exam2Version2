package dsf19Exam2Version2;

public class cscs {

	/*
	 * Consider the following code fragement:
	 * 
	 * void printList(SinglyLinkedList<Integer> L) {
	 * 
	 * Integer num = null;
	 * 
	 * for (int i=0; i < L.size(); ++i){
	 * 
	 * num = L.get(i);
	 * 
	 * System.out.println(num);
	 * 
	 * }
	 * 
	 * }
	 * 
	 * What is the time complexity for this code? Select one: a. O(1) b. O(N2),
	 * where N = L.size() c. O(N), N = L.size() d. O(N*M), where N = L.size(), and M
	 * = i e. None of the alternatives is right. Feedback Your answer is incorrect.
	 * The correct answer is: O(N2), where N = L.size()
	 */

}
